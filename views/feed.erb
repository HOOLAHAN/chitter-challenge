<!DOCTYPE html>
<html lang="en">
  <head>
  <title>Chitter</title>
    <link rel="stylesheet" href="/style.css" />
    <link rel="preconnect" href="https://fonts.googleapis.com" />
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin />
    <link
      href="https://fonts.googleapis.com/css2?family=Roboto:wght@400;700&display=swap"
      rel="stylesheet"
    />
  </head>
  <body>
    <header>
      <nav class="navbar">
        <a href="/"><img class="logo" src="chitter.png" alt="logo" /></a>
        <a href="/"><img class= "logo2" src="chitter_title.png" alt="Chitter" /></a>
        <div class="navbar">
          <% if session[:user_id] == nil %>
            <a class="nav__link" href="/login">Login</a>
            <a class="nav__link" href="/new_user">Create account</a>
          <% else %>
            <a class="nav__link" href="/feed/new">Post a new Peep</a><br />
            <a class="nav__link" href="/logout">Logout</a>
          <% end %>
          </div>
      </nav>
  </header>
  <center><div>
    <% @peeps.reverse.each do |peep| %>
      <div class="container">
      <h2><%= peep.content %></h2>
      <a class="search-btn" href="/feed/<%= peep.id %>">
        Posted at <%= peep.timestamp.split(' ').last.split(':').slice(0,2).join(':') %> 
        on <%= peep.timestamp.split(' ').first %>
      </a>
      </div>
    <% end %>
  </div></center>
</html>
